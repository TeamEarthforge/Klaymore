# Klaymore 脚本编写速启指南

> 面向脚本作者，5 分钟上手。基于 Kotlin，运行于 Minecraft 1.7.10 Forge。

---

## 一、脚本放在哪？

脚本目录（`<存档>/klaymore/`）结构：

```
klaymore/
├── server/          ← 服务端脚本（逻辑、规则、世界操作）
│   └── Root.kt      ← 服务端入口，必须存在
└── client/          ← 客户端脚本（HUD、渲染、按键）
    └── Root.kt      ← 客户端入口（可选）
```

- **Root.kt** 是入口脚本，引擎启动时自动挂载。
- 其他脚本由 Root.kt 通过 `spawnChild` 按需加载。
- 服务端/客户端脚本独立运行，互不干扰。
- **同一目录下的脚本可以互相引用类**（见第七节）。

---

## 二、最小脚本骨架

所有脚本必须继承 `KlaymoreScript` 基类：

```kotlin
import com.earthforge.klaymore.script.KlaymoreScript
import com.earthforge.klaymore.script.Subscribe
import net.minecraft.util.ChatComponentText

class MyScript : KlaymoreScript() {

    // 自定义状态
    private var counter = 0

    // 事件订阅：注解 + 单参方法
    @Subscribe(net.minecraftforge.event.entity.player.PlayerEvent::class)
    fun onPlayerLogin(event: net.minecraftforge.event.entity.player.PlayerEvent) {
        if (event.entityPlayer == target) {
            counter++
            container.setPersistent("loginCount", counter)
            event.entityPlayer.addChatMessage(
                ChatComponentText("欢迎回来！这是你第 $counter 次登录。"))
        }
    }
}
```

### class 还是 object？

| 声明方式 | 适用场景 | 说明 |
|---|---|---|
| `class Foo : KlaymoreScript()` | **推荐**，需要多实例的脚本 | 每次 `spawnChild` 生成独立实例，各自有独立的 target / 数据 |
| `object Foo : KlaymoreScript()` | 单例脚本，如 Root、System | 全局只有一个实例，适合不绑定具体目标的全局脚本 |

> 绝大多数脚本应该用 `class`。只有确实只需要一个实例的全局脚本才用 `object`。

### 注入字段

`KlaymoreScript` 基类已声明以下字段，引擎自动注入，**无需手动声明**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `target` | `Any` | 绑定的目标对象（玩家、实体、方块、Dummy 等） |
| `container` / `selfContainer` | `ScriptContainer` | 当前容器（存数据、卸载等） |
| `parent` | `Any?` | 父容器的目标对象（可能为 null） |
| `net` | `ScriptNet` | 网络通信通道 |

### 全局脚本

不绑定具体 target 的脚本（如 Root、System），覆写 `isGlobal` 为 `true`，引擎会自动分配一个 `Dummy` 作为 target：

```kotlin
class Root : KlaymoreScript() {
    override val isGlobal = true
    // ...
}
```

---

## 三、事件订阅

```kotlin
@Subscribe(事件类::class)
fun onXxx(event: 事件类) {
    // 仅当事件来源 == target 时才会触发（引擎自动过滤）
}
```

### 要点
- 注解参数 = 事件类，方法参数类型必须与之兼容。
- 引擎只将事件派发给 `target` 匹配的脚本，无需自己判断 `event.entity == target`（但判断更安全）。
- 只支持实例方法，不支持静态方法。
- 一个脚本可订阅多个事件，一个事件可被多个脚本订阅。

### 常用事件示例

```kotlin
// 玩家聊天
@Subscribe(net.minecraftforge.event.ServerChatEvent::class)
fun onChat(event: net.minecraftforge.event.ServerChatEvent) { ... }

// 玩家受伤
@Subscribe(net.minecraftforge.event.entity.living.LivingHurtEvent::class)
fun onHurt(event: net.minecraftforge.event.entity.living.LivingHurtEvent) { ... }

// 方块破坏
@Subscribe(net.minecraftforge.event.world.BlockEvent.BreakEvent::class)
fun onBlockBreak(event: net.minecraftforge.event.world.BlockEvent.BreakEvent) { ... }
```

---

## 四、数据存储

`ScriptContainer` 提供两类数据：

```kotlin
// 临时数据：内存中，重启丢失
container.setTemp("key", value)
val v = container.getTemp("key")

// 持久化数据：随容器落盘到 bindings.json，重启恢复
container.setPersistent("level", 5)
val level = container.getPersistent("level") as? Int ?: 1

// 导出全部持久化数据（保存时引擎自动调用）
val data = container.exportPersistentData()
```

> 持久化数据仅支持基本类型（String / Number / Boolean / List / Map），复杂对象会被警告且可能无法序列化。

---

## 五、网络通信（ScriptNet）

```kotlin
// 发送
net.sendToServer("channel_name", mapOf("x" to 1, "y" to 2))   // 客户端 → 服务端
net.sendToAll("channel_name", data)                              // 服务端 → 所有玩家
net.sendToPlayer(player, "channel_name", data)                   // 服务端 → 指定玩家

// 接收
net.on("channel_name") { data, sender ->
    val x = data["x"] as? Double ?: 0.0
    println("收到来自 ${sender?.displayName} 的消息: x=$x")
}
```

- `data` 反序列化为 `Map<String, Any?>`。
- handler 跟随容器生命周期，卸载时自动清理，无需手动反注册。
- handler 统一在主线程执行，可安全操作世界对象。

---

## 六、生成子实例（蓝图复用）

同一份脚本可生成多个运行时实例，每个绑定不同目标与路径。

```kotlin
import com.earthforge.klaymore.script.ScriptContainerFactory

// 在父脚本中：
val child = ScriptContainerFactory.spawnChild(
    parentContainer = container,
    scriptName      = "Soldier.kt",     // 必须已编译缓存
    target          = someEntity,       // 绑定的实体
    path            = "/game/red/soldier_1",  // 逻辑路径
    extraPersistentData = mapOf("team" to "red")
)

child?.run("attack", targetEntity)  // 调用子脚本的方法
```

### 路径（Path）
- 路径是字符串，如 `/game/red/soldier_1`，随持久化数据落盘。
- 父子关系由路径前缀动态推导：`/game/red/soldier_1` 是 `/game/red` 的子级。
- 不写在脚本源码里，由父脚本在 `spawnChild` 时传入。

---

## 七、同目录脚本互相引用

同一目录下的所有 `.kt` 脚本会作为一个编译单元一起编译，因此**同目录内的类可以直接互相引用**，无需通过 FactBase 或 ContainerIndex 间接通信。

### 示例

```
klaymore/server/
├── Root.kt
├── Soldier.kt
└── Squad.kt
```

**Soldier.kt：**
```kotlin
import com.earthforge.klaymore.script.KlaymoreScript

class Soldier : KlaymoreScript() {
    fun attack(target: Any) {
        // ...
    }
}
```

**Squad.kt：**
```kotlin
import com.earthforge.klaymore.script.KlaymoreScript

class Squad : KlaymoreScript() {
    private val soldiers = mutableListOf<Soldier>()  // 直接引用同目录的 Soldier 类

    fun addSoldier(s: Soldier) {
        soldiers.add(s)
    }
}
```

### 规则

- **同目录**：可以直接引用彼此的类、方法、属性（取决于可见性修饰符）。
- **跨目录**：不能直接引用，需通过 `FactBase`、`ContainerIndex` 或网络通信间接交互。
- 耦合紧密的脚本应放在同一目录；需要解耦的脚本分目录放置。

---

## 八、查找与通信进阶

### ContainerIndex —— 通过 Key 快速定位容器

```kotlin
import com.earthforge.klaymore.script.ContainerIndex
import com.earthforge.klaymore.script.PersistenceManager

val key = PersistenceManager.generateKey(entity)  // "entity:uuid-xxx"
val c = ContainerIndex.get(key)                   // O(1) 查找
c?.run("doSomething")
```

### FactBase —— 三元组事实库（解耦通信）

替代广播事件，System 脚本写事实，棋子脚本按 Predicate 查询。适用于**跨目录**或**松耦合**的脚本间通信。

```kotlin
import com.earthforge.klaymore.script.FactBase

// System 脚本写入指令
FactBase.put(soldierKey, "cmd.move", mapOf("x" to 100, "z" to 200))

// 棋子脚本查询与自身相关的指令
val facts = FactBase.queryByPredicate("cmd.move")
for ((subject, _, obj) in facts) {
    if (subject == myKey) {
        val coord = obj as? Map<*, *>
        // 执行移动
    }
}
```

> 同目录脚本优先用直接引用（第七节），跨目录或解耦场景才用 FactBase。

---

## 九、Root.kt 示例

```kotlin
import com.earthforge.klaymore.script.*
import net.minecraft.util.ChatComponentText

class Root : KlaymoreScript() {
    override val isGlobal = true

    init {
        println("[Root] 服务端根脚本已加载")
    }

    // 监听玩家登录，为每个玩家挂载一个 PlayerScript 实例
    @Subscribe(net.minecraftforge.event.entity.player.PlayerEvent::class)
    fun onPlayerJoin(event: net.minecraftforge.event.entity.player.PlayerEvent) {
        val player = event.entityPlayer ?: return
        val key = PersistenceManager.generateKey(player) ?: return

        if (ContainerIndex.get(key) != null) return  // 已挂载

        ScriptContainerFactory.spawnChild(
            parentContainer = container,
            scriptName      = "PlayerScript.kt",
            target          = player,
            path            = "/players/${player.displayName}"
        )
        player.addChatMessage(ChatComponentText("[Klaymore] 玩家脚本已挂载"))
    }
}
```

---

## 十、常见问题

**Q: 脚本不生效？**
1. 检查文件位置：服务端脚本放 `server/`，客户端放 `client/`。
2. 查看日志中 `[Klaymore Script]` 开头的编译错误。
3. 确认 `Root.kt` 存在且无语法错误。
4. 确认脚本类继承了 `KlaymoreScript`。

**Q: `lateinit var target` 未初始化？**
- 不要在 `init` 块或属性初始化时访问 `target`，此时注入尚未完成。
- 在事件回调或方法中访问即可。

**Q: 如何重新加载脚本？**
- 使用游戏内命令 `/klaymore reload`（需 mod 提供）。
- 或重启服务器/客户端。

**Q: 脚本间如何调用？**
- **同目录**：直接引用类、创建实例、调用方法。
- **跨目录**：通过 `ContainerIndex.get(key)` 获取容器，再 `container.run("方法名", 参数...)`；或通过 `FactBase` 写入事实。

**Q: 服务端脚本和客户端脚本如何通信？**
- 使用 `net.sendToServer` / `net.sendToAll` 等网络通道。
- 服务端脚本只能在服务端运行，客户端脚本只能在客户端运行，不能直接调用。

**Q: 一个脚本可以被多次挂载吗？**
- 用 `class` 声明的脚本可以，每次 `spawnChild` 生成独立实例。
- 用 `object` 声明的脚本是单例，多次挂载会共享同一实例。

---

## 十一、API 速查表

| 功能 | API |
|------|-----|
| 脚本基类 | `class Foo : KlaymoreScript()` |
| 全局脚本 | `override val isGlobal = true` |
| 事件订阅 | `@Subscribe(EventClass::class)` |
| 临时数据 | `container.getTemp/setTemp` |
| 持久数据 | `container.getPersistent/setPersistent` |
| 网络发送 | `net.sendToServer/sendToAll/sendToPlayer` |
| 网络接收 | `net.on(channel) { data, sender -> }` |
| 生成子实例 | `ScriptContainerFactory.spawnChild(...)` |
| Key 生成 | `PersistenceManager.generateKey(target)` |
| 容器查找 | `ContainerIndex.get(key)` |
| 事实写入 | `FactBase.put(subject, predicate, obj)` |
| 事实查询 | `FactBase.queryByPredicate(predicate)` |
| 调用脚本方法 | `container.run("method", args...)` |
| 获取路径 | `container.path` |
| 卸载容器 | `ScriptContainerFactory.unmount(container)` |
